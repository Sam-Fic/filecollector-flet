#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SRC_DIR="$ROOT_DIR/src"
export PYTHONPATH="$SRC_DIR${PYTHONPATH:+:$PYTHONPATH}"
cd "$SRC_DIR"
exec python3 -m filecollector --flet "$@"
